# README Setup

This profile README uses:
- GitHub dynamic statistics
- GitHub streak statistics
- Top-language statistics
- LeetCode statistics
- GitHub activity graph
- Contribution snake animation

Snake animation:
1. Push `.github/workflows/snake.yml` to the `main` branch.
2. Open GitHub → Actions → Generate Contribution Snake.
3. Run the workflow once manually.
4. The workflow creates/updates the `output` branch.
5. The README then loads the generated SVGs from that branch.

Important:
- The LeetCode card is configured for `kamalesh-thirunavukkarasu-s`.
- If that is not the exact LeetCode username, change it in `README.md` before pushing.
- Dynamic third-party cards can occasionally be rate-limited; they are supplemental to the direct profile links.
